<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".uploadpost.UploadPostActivity">
    <include layout="@layout/titlebar_uploadpost"/>
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            >
            <EditText
                android:id="@+id/upload_post_content"
                android:layout_width="match_parent"
                android:layout_height="90dp"
                android:padding="5dp"
                android:hint="在此输入您想要分享的文字内容"
                android:inputType="textMultiLine"
                android:minLines="5"
                android:gravity="top"
                android:theme="@style/MyEditText"
                android:background="@drawable/posttext_input_gb"
                android:layout_marginTop="5dp"
                android:layout_marginLeft="5dp"
                android:layout_marginRight="5dp"
                android:maxLength="120"
                />
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/upload_post_imgs"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">
            </androidx.recyclerview.widget.RecyclerView>
        </LinearLayout>
    </ScrollView>
</LinearLayout>