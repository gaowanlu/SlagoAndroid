<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="site.linkway.slago">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BigWorks"
        android:usesCleartextTraffic="true">
        <activity android:name="site.linkway.slago.search.SearchActivity"></activity>
        <activity android:name="site.linkway.slago.more.UserAgreementActivity" />
        <activity android:name="site.linkway.slago.login.CaptchaActivity" />
        <activity android:name="site.linkway.slago.login.ChangePasswordActivity" />
        <activity android:name="site.linkway.slago.login.RegisteredAccountActivity" />
        <activity android:name="site.linkway.slago.login.ForgetPasswordActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="site.linkway.slago.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"
                />
        </provider>

        <activity android:name="site.linkway.slago.postpage.VisitorActivity" />
        <activity android:name="site.linkway.slago.postpage.PostActivity" />
        <activity android:name="site.linkway.slago.login.LoginActivity" />
        <activity android:name="site.linkway.slago.uploadpost.UploadPostActivity" />
        <activity android:name="site.linkway.slago.persondata.MySexActivity" />
        <activity android:name="site.linkway.slago.persondata.PersonalSignatureActivity" />
        <activity android:name="site.linkway.slago.persondata.MyName" />
        <activity android:name="site.linkway.slago.postpage.UserPeronalActivity" />
        <activity android:name="site.linkway.slago.more.MorePageActivity" />
        <activity android:name="site.linkway.slago.achievement.AchievementPageActivity" />
        <activity android:name="site.linkway.slago.persondata.PersonDataPageActivity" />
        <activity android:name="site.linkway.slago.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>

</manifest>